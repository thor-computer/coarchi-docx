# coarchi-docx

Workflow that extracts information from DOCX to update coArchi diagram

## Configuration

To use this workflow, you must configure the URL of the repository containing the coArchi model in `config.yml`. Update the `model_repository_url` key with the URL of the repository.
